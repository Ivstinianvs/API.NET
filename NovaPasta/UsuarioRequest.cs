using System;
using System.Collections.Generic;
using System.Linq;
using System.Threading.Tasks;

namespace SolidCursos.NovaPasta
{
    public class UsuarioRequest
    {

        var usuario = new Usuario(Usuario)
            usuario.Nome = UsuarioRequest.Nome;
            usuario.Login = UsuarioRequest.Login;
            usuario.Senha = UsuarioRequest.Senha;
            usuario.Email = UsuarioRequest.Email private class Email
        {
        }

        private class Senha
        {
        }
        private class Nome
        {
        }

        private class Login
        {
        }
    }
}
